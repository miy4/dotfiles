# -*- mode: snippet -*-
# condition: (or (eq sh-shell 'bash) (eq sh-shell 'zsh))
# name: script directory
# key: scriptdir
# --
_SCRIPT_DIR="$(cd "$(dirname "`(cond ((eq sh-shell 'bash) "${BASH_SOURCE[0]}")
                                     ((eq sh-shell 'zsh) "${(%):-%N}"))`")" && pwd)"
