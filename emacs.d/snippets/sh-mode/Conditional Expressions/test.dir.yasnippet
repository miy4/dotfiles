# -*- mode: snippet -*-
# name: test if file exists and is a directory
# key: dir?
# group: Conditional Expressions
# --
`(cond ((or (eq sh-shell 'bash) (eq sh-shell 'zsh)) "[")))`[ -d ${1:file} ]`(cond ((or (eq sh-shell 'bash) (eq sh-shell 'zsh)) "]")))`